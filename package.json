{
  "name": "root",
  "private": true,
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  },
  "scripts": {
    "build": "lerna run build",
    "test": "lerna run test",
    "lint": "lerna run lint"
  },
  "dependencies": {},
  "devDependencies": {
    "lerna": "^7.4.2",
    "syncpack": "^11.2.1"
  }
}
